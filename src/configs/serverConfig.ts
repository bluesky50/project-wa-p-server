const serverConfig = {
	appName: 'pairs',
	port: 5000,
	host: 'localhost',
	env: 'dev',
	dbUri: 'mongodb://localhost/',
	gqlEndpoint: '/graphql',
	gqlSubscriptionsEndpoint: '/subscriptions'
}

export default serverConfig;
